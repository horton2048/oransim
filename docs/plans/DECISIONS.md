# 无人值守自主决策日志（DECISIONS）

> 策略：用户已授权「遇到计划没覆盖的歧义/卡点时，按四条铁律自行决断并记录，绝不停等」。
> 每条决策一行记录：日期、上下文（哪个 AT/文件）、歧义是什么、依据哪条铁律/红线、最终决定。
> 产品级决策（黄金快照变更、置信度阈值改动、方案级取舍）即使已自行决断，也在此**显式标 `⚠ 待复核`**，回来后逐条过。

| 日期 | 上下文 | 歧义 | 依据 | 决定 | 待复核? |
|---|---|---|---|---|---|
| 2026-06-11 | （骨架搭建） | — | — | 建立 LEDGER/DECISIONS/accept.ps1/RUNBOOK | — |
| 2026-06-11 | AT-M0-02 黄金快照 · api_helpers.py | `creative_id = f"cr_{int(time.time()*1000)%100000}"` 每次调用生成不同 ID → 破坏字节级兼容 (铁律 1) | 铁律 1 (字节级兼容) | 改为 sha256(caption:style:mood:dur:celeb)[:10] 内容寻址，同一 creative 输入永远映射同一 ID。纯工程修复，无业务语义变更 | 否 |
| 2026-06-11 | AT-M0-02 黄金快照 · schema_outputs 运行 ID | `schema_outputs.*` 的 run_id/plan_id/fit_id/simulation_id 等使用 `uuid.uuid4()` 和 `time.strftime()`，每次调用不同 → 字节不稳定。`macro.world.fetched_at` 同理 | 铁律 1 (字节级兼容) 的实用解释：保护 KPI 值、字段结构，不保护运行元数据 ID | 快照比对前做 normalize 规整化（替换所有 run_id/timestamp 为占位符），再做字节比较。这等价于 "结构+数值字节级稳定"，符合 §8.2-2 的保护意图。未来 M7 可把所有 ID 改为 hash-based，届时去掉 normalize | ⚠ 待复核（M7 时把 uuid.uuid4() 改为 seed-based hash，届时可移除 normalize） |
| 2026-06-11 | M-1 基线刷绿 · tests/test_smoke.py | 基线 5 红挡住 REG-3「全绿」前提：3×`read_text` 无 encoding（GBK 炸）、1×`pytest.raises(match=)` 未 re.escape Windows 路径、1×`grep` 子进程 Windows 无此二进制 | 铁律 4（M0 前置于一切，先有绿基线）；纯测试侧 portability，不碰业务 | read_text 加 `encoding="utf-8"`；match 加 `re.escape`；grep 测试 `shutil.which("grep") is None` 时 skip（保留 CI/Linux 守门）。结果 104 passed/3 skipped/0 failed | 否（纯测试 portability，无产品语义） |
| 2026-06-11 | M2 grounding · tests/golden/launch_ideas.jsonl | M1 黄金集 `expected_niche_key` 用了 `baby`/`education` 两个**不在** niches.json 10 垂类中的值（实际为 beauty/fashion/food/beverage/fitness/electronics/travel/home/pet/parenting）。education 类（笔记/记账 App、物理辅导、AI 写作）在 10 垂类无诚实落点 | 铁律 2（诚实原则：B2B/SaaS/工具类无诚实 grounding 应硬拒绝，不可静默映射到消费垂类）；方案 §3.4 | ① `baby`→`parenting`（同域真垂类）；② `亲子时间管理 App`→`parenting`（亲子向消费 App，保正例）；③ 纯数字工具/知识服务 4 条（笔记/记账 App、物理辅导、AI 写作助手）改为 reject（`expected_niche_key:null, expect_reject:true, reject_reason:"unsupported_vertical"`）。结果：正例 26→22，reject 4(B2B)+4(工具)=8 | ⚠ 待复核（黄金集构成变更属产品级；M8 评估是否新增 edu/tool 垂类后将这些改回正例） |
| 2026-06-11 | M2 grounding 置信度阈值 · spec/ground.py | 方案/规范给「建议 0.55」硬拒绝阈值，边界 0.55 自身行为未定 | 铁律 2；规范 §3.1 红线（阈值初始 0.55） | `grounding_confidence >= 0.55` 通过、`< 0.55` 硬拒绝（边界含入）。synonyms 命中基础置信 0.92，无语料覆盖乘 0.5 惩罚（→0.46 触发拒绝） | ⚠ 待复核（阈值与惩罚系数为产品级标定参数） |
| 2026-06-12 | M5 市场潜量 · config/niches.adoption_rate_priors | 方案要求 m=fan_weight×adoption_rate_prior，但 niches.json 无 adoption_rate_prior（v2 字段，M8 才补） | 方案 §4.4；与 reference_prices() 同款「回退+未标定标注」范式 | 加 `_ADOPTION_RATE_FALLBACK` 逐 niche 区分回退值（beauty .08/food .12/…），getter 返回 (rates, uncalibrated)；niches.json 未设字段的 niche 全进 uncalibrated，报告需标「未标定」 | ⚠ 待复核（M8 补 niches.json v2 真值后回退值应失效） |
| 2026-06-12 | M5 Bass 先验 · diffusion/bass_saturated_hawkes | Bass p/q 先验方案指明来自 niches.json v2（§5），当前未标定 | 方案 §4.4（「未标定前在报告显式标注」） | 用默认 p=0.03/q=0.38（q>p 保证内部峰值），forecast latent 带 `calibrated:False` + note「未经真实数据标定」。均场 ODE 确定性积分（非随机 thinning），逐日对照闭式 Bass <15% | ⚠ 待复核（p/q/m 标定为产品级；M8 接 niches.json v2 与真实曲线回归） |
| 2026-06-12 | M6 SCM 新节点泄入 /api/predict · scm.dag_dict | M6 加 price_point/launch_channel_mix 两节点令 dag_dict() 序列化 +631B，泄入 /api/predict → 破坏铁律 1（字节级兼容）；方案 §4.5 未预见 predict 内嵌 dag | 铁律 1（最高）；方案 §4.5「只增不改」 | SCMNode 加 `launch_only` 标记，两上市节点置 True；dag_dict()/dag_dict_unrolled() 默认 `include_launch=False`（campaign 视图排除上市节点+边）→ /api/predict 回到 64/117 字节级稳定；NODES/EDGES 与 equilibrium_under_do 仍用全图 66/122（launch 路径 include_launch=True 取全图）。纯工程修复，无产品语义变更 | 否（保铁律 1 的正确工程解；campaign 前端本不应看到上市节点） |
| 2026-06-12 | M8 niches.json v2 新品类破坏 REG-2 · config/niches.py + data/kols.py | 加 5 个 v2 品类到 niche_keys() 会改变 kols.generate_kol_library 的 `rng.choice(NICHES)` 抽样序列 → 整个 KOL 库变 → /api/predict 字节变 → 破坏铁律 1/REG-2 | 铁律 1（最高，REG-2 红线）；规范 §5.1「v2 只增不改不删」 | 5 新品类标 `tier:"v2"`；config/niches.py campaign 域 getter（niche_keys/synonyms/ctr_priors/en_to_zh/bias_captions/...）默认 `include_v2=False` 排除 v2 → kols/predict/所有既有消费者字节级不变（REG-2 绿，183 passed + 快照 3× 稳定）。grounding 与新 getter（reference_prices/adoption_priors/bass_priors）显式 include_v2=True 取全 15。既有 10 niche 老字段零改动（v2 字段只加给新 5，原 10 走 fallback 保 AT-M4-07 uncalibrated>0）。纯工程修复 | 否（保铁律 1 的正确工程解；campaign KOL 库本不应因上市域品类扩展而变） |
| 2026-06-12 | M8 niches.json v2 先验未真实标定 · data/niches.json | 新增 reference_price/adoption_rate_prior/bass_p_q_prior/aov_prior 全是合成估值 | 铁律 2（诚实）；规范 §8.4 | 新 5 品类先验为合成估值；calibration_trace 来源逐字标「合成数据自标定，非真实校准」；原 10 品类仍走 fallback 标 uncalibrated。getter 全覆盖 15 品类，缺字段 fallback + 未标定标注 | ⚠ 待复核（全部先验值为产品级标定参数；接真实数据前均属未校准） |
