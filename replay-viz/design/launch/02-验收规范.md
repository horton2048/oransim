# 上市回放前端 · 验收规范

> 承接 `00-工作流总纲.md` / `01-开发规范.md`。本文件定义「做完了」的**客观判据**。
> HYBRID：M1 数据线全自动（pytest 逐字节）；M2–M4 像素/集成走 Playwright + 人工清单。
> 命名 `AT-FE-<里程碑>-<序>`。每条独立判红/绿。诚实类红线是**可门禁的硬条件**，不可豁免。

---

## FE-M1 · 适配器（全自动，pytest，确定性）

测试落 `tests/test_launch_replay_export.py`，mock，`PYTHONHASHSEED=0`，输入 `fixtures/launch_sample.json`。

| AT | 判据 | 类型 |
|---|---|---|
| **AT-FE-1-01** 确定性 | 同输入连跑 2 次输出**逐字节相同** | 硬 |
| **AT-FE-1-02** 黄金一致 | `export()` 输出 == `fixtures/launch_replay.golden.json`（逐字节） | 硬 |
| **AT-FE-1-03** 过 validReplay | 输出满足 v3 `validReplay()`：`version===1` + `kpis` + `budget`是数字 + `cities`非空 + `daily_total`非空 + `kols/souls`是数组 + `funnel_percentiles.{A1,A3,A4}` + `per_platform` | 硬 |
| **AT-FE-1-04** 脊柱真值不被篡改 | `daily_total[i] === timeline.daily_adopters[i]`（逐点相等，无平滑/裁剪/补值/重排）；`meta.peak_day === timeline.peak_day` | 硬（诚实） |
| **AT-FE-1-05** 分位带直通 | `metrics.*.{p35,p50,p65}` 原样附带，无重排、无 p35>p50>p65 倒挂 | 硬（诚实） |
| **AT-FE-1-06** 干预卡直通 | `intervention_cards[]` 的 `{name,label,delta,branch,note}` 逐字段等于报告；`note` 逐字不润色 | 硬（诚实） |
| **AT-FE-1-07** 合成字段全打标 | cities / souls.t / souls.city / daily_paid 等所有合成项出现在 `meta.synthetic_fields[]` | 硬（诚实） |
| **AT-FE-1-08** 城市层级守真 | 各 tier 的 `Σweight` 占比 == `effective_city_dist`（误差 < 0.5pt）；未命中进 `unmapped_cities` 非静默丢 | 硬 |
| **AT-FE-1-09** 证词原话逐字 | `souls[].text` 与对应 `persona_quotes[].objection` 逐字相等（非空时） | 硬（诚实） |
| **AT-FE-1-10** sentiment 派生口径 | `souls[].sentiment` 按 §3.4 分档（intent≥0.5→1 / ≤0.05→-1 / else 0）可复现 | 硬 |
| **AT-FE-1-11** 诚实元上屏可用 | 输出含 `meta.disclaimer`（==报告原句）+ `grounding_confidence` | 硬（诚实） |
| **AT-FE-1-12** 合成确定性 | 合成的 `souls[].t` / `cities` 指派在同输入下逐字节稳定 | 硬 |

**FE-M1 DoD**：AT-FE-1-01..12 全绿（硬条目无一红）。验收闸 `pwsh replay-viz/scripts/accept-fe.ps1`。

---

## FE-M2 · 接 v3 取数（Playwright + 人工）

| AT | 判据 | 方式 |
|---|---|---|
| **AT-FE-2-01** fetch 通路 | v3 加载适配器产出的 `app/replay.json`，**走 fetch 非内嵌兜底**（无水印），console 无错 | [自] |
| **AT-FE-2-02** 点阵非空 | 城市点阵渲染、光点数 ∝ weight，城市数 == `cities.length` | [自/人] |
| **AT-FE-2-03** 五幕可播 | 点「开始回放」→ 五幕按 90 天节拍切换，幕标题/旁白随 `peak_day=6` 等真值出现 | [人] |
| **AT-FE-2-04** 时间轴 | 拖动游标与曲线/幕同步，上限 == `daily_total.length`(=90) | [人] |
| **AT-FE-2-05** 证词卡 | 点光点弹卡，显 `objection` 原话 + 意向；**合成项（城市/时刻）有微标** | [人]（诚实） |
| **AT-FE-2-06** 离线兜底 | 断网/无 replay.json → 内嵌样本仍播完五幕、不黑屏、显水印 | [人] |

**FE-M2 DoD**：AT-FE-2-01..06 全过。

---

## FE-M3 · 判决面板纠偏（决策 B，视觉/Playwright）

| AT | 判据 | 方式 |
|---|---|---|
| **AT-FE-3-01** 标签纠偏 | 判决面板显「触达 / 试用 / 采纳」+「**P35/P50/P65**」，**无** campaign 残留的「A1/A3/A4」「p25/p75」误标 | [人]（诚实） |
| **AT-FE-3-02** 读真 metrics | 三条带数值 == `metrics.{unique_reach,trials,adopters}.{p35,p50,p65}`；GMV 带 == `metrics.revenue.*` | [人/自] |
| **AT-FE-3-03** 弱字段标注 | `per_platform` ROI / 口碑净情绪无真值处显「—」或隐藏，不编造 | [人]（诚实） |
| **AT-FE-3-04** 第五幕旁白 | 旁白为 P35/P50/P65 口径，可含 `saturation_date`/`market_potential_m` | [人] |
| **AT-FE-3-05** campaign 不回归 | 喂 campaign `replay.json`（有 `funnel_percentiles` 无 `metrics`）时判决面板走旧逻辑、显示如初 | [人]（薄改） |

**FE-M3 DoD**：AT-FE-3-01..05 全过。

---

## FE-M4 · 并入主 SPA（人工 DoD）

| AT | 判据 | 方式 |
|---|---|---|
| **AT-FE-4-01** 挂载 | 主 SPA 出现「🎬 战况回放」第 10 tab，iframe 加载 v3，不破坏其它 9 tab | [人] |
| **AT-FE-4-02** 数据贯通 | 主 SPA 当前 `spec_id` → simulate → `GET /api/launch/replay/{spec_id}` → 回放，caption 对得上 | [人] |
| **AT-FE-4-03** 资产落位 | `app/` 复制进 `frontend/replay/`，Three.js 自托管 `vendor/three/`，断网不黑屏 | [人] |
| **AT-FE-4-04** 不回归 | 既有 9 tab + :8090 生产前端不受影响；M1 pytest 仍全绿 | [人/自] |
| **AT-FE-4-05** R5 确认 | `oransim/api.py` 已确认 session/store 持有完整 `LaunchReport`（或路由内重建），DECISIONS 记一行 | [人] |

**FE-M4 DoD**：AT-FE-4-01..05 全过。

---

## 总门禁
- 任一**诚实类**（标「诚实」）AT 红 = 整里程碑不算过，不可豁免。
- 一里程碑全绿再进下一个。
- 黄金（`launch_replay.golden.json`）变更必须人工核对 + DECISIONS 记录，**绝不在循环里顺手重生**。
